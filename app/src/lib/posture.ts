/**
 * 顶栏品牌文案与产品隐私口径的集中出处。
 *
 * 「所有数据都在本地」在接了外部 API 之后是过度承诺 —— 文本会出去。
 * 所以口径收紧成三层，写文案时按场合取用：
 *   默认态   音频不上传
 *   默认态   转写、分析、知识索引默认全在本机
 *   选择权   第三方转写和分析逐处独立同意，分析模型只发送文本
 * 改口径只改这里，三处引用自动同步。
 */
export const PRIVACY_POSTURE = {
  /** 顶栏品牌区副标题：表达产品的长期价值 */
  toolbar: "让每次对话，都沉淀为长期记忆",
  /** 引导向导第 0 步清单：把「全部留在本机」收紧为默认态 */
  onboardingFirstItem: "音频默认不上传；逐字稿与分析默认留在本机",
} as const;
