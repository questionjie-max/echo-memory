/**
 * 产品隐私口径的唯一出处。
 *
 * 「所有数据都在本地」在接了外部 API 之后是过度承诺 —— 文本会出去。
 * 但有一条代码里可验证的硬承诺始终成立：音频永不上传。
 * 所以口径收紧成三层，写文案时按场合取用：
 *   硬承诺   音频永不上传
 *   默认态   转写、分析、知识索引默认全在本机
 *   选择权   接外部 API 时只发文本，且逐处显式开启
 * 改口径只改这里，三处引用自动同步。
 */
export const PRIVACY_POSTURE = {
  /** 顶栏品牌区副标题：一行说清硬承诺与例外 */
  toolbar: "音频永不上传 · 外部 AI 只发文本",
  /** 引导向导第 0 步清单：把「全部留在本机」收紧为默认态 */
  onboardingFirstItem: "音频永不上传；逐字稿与分析默认留在本机",
} as const;
